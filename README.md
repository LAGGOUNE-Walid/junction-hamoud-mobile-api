# junction-hamoud-mobile-api
### Install and use
```
docker-compose build
docker-compose up -d
docker-compose exec phpjunction bash
php artisan migrate
```
The project will be available at http://127.0.0.1:82/admin
